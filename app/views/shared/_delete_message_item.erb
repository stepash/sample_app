<% if current_user?(item_obj.user) %>
 <%= link_to "delete", item_obj, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: item_obj.content %>
 <% end %>